server {
    listen 80;
    server_name localhost;

    # Next.js 정적 파일
    location ~ ^/_next/static/(.*) {
        root /home/intellio_korea/intellio_prod/frontend/main/.next;
        try_files /static/$1 =404;
        expires 365d;
        access_log off;
        add_header Cache-Control "public, no-transform";
    }

    # Main 페이지 (port 3000)
    location / {
        proxy_pass http://host.docker.internal:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # FastAPI 백엔드 API
    location /api/ {
        proxy_pass http://host.docker.internal:8000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# DocEasy 서비스
server {
    listen 80;
    server_name doceasy.localhost;

    location / {
        proxy_pass http://host.docker.internal:3010;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# StockEasy 서비스
server {
    listen 80;
    server_name stockeasy.localhost;

    location / {
        proxy_pass http://host.docker.internal:3020;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
