---
description: 
globs: 
alwaysApply: false
---
# Your rule content
- 에러는 근본 원인을 가장 우선시 고민할것. 차선으로 에러 시 예외처리 수행.
- COT(Chain of Thought)에 근거하여 답변해
- 주석문을 마음대로 삭제하지 말것.
- 주석문은 최대한 자세하게 작성하고 코드 변경시 주석도 함께 변경할것.
- 기능 수정, 버그 수정 시 함수의 리턴값은 변경하지 말것. 변경이 필요하다면, 사용자에게 질문할 것.
- 추상화 단계를 너무 깊숙히 가져가지 말것.
- 불필요한 추상화 금지
- 모든 터미널의 명령은 PowerShell 명령어를 사용하며, ssh환경에서는 리눅스 명령어를 사용한다.
- 로그는 가능한 한글로 작성한다.
- 코드 실행 금지. 직업 진행함.

# 금지사항
- 기존에 사용하던 변수 이름 변경 금지.
- 코드 최적화 금지

# SSH, Cloud Server
- Main : port 3000 ( www.intellio.kr )
- Doceasy : port 3010 ( doceasy.intellio.kr )
- Stockeasy : port 3020 ( stockeasy.intellio.kr ) 

# response formatter
- Gemini의 @tool 데코레이터는 파라미터로 dictionary를 넘겨야한다.
- 정상 : create_paragraph({"content": before_text})
- 오류 : create_paragraph(content=before_text)